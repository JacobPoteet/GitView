{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "GitView talks to its own Rust commands, plus the native folder picker.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
